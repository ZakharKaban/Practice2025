<Page x:Class="Practice2025One.Pages.SuppliersManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Управление поставщиками"
      Loaded="Page_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Панель управления -->
        <Border Grid.Row="0" Background="White" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="Добавить поставщика" 
                       Click="AddSupplierButton_Click"
                       Padding="15,10" Margin="0,0,10,0"
                       Background="#4CAF50"
                       Foreground="White"/>
                <Button Content="Редактировать" 
                       Click="EditSupplierButton_Click"
                       Padding="15,10" Margin="0,0,10,0"
                       Background="#2196F3"
                       Foreground="White"/>
                <Button x:Name="DeleteSupplierButton"
                       Content="Удалить" 
                       Click="DeleteSupplierButton_Click"
                       Padding="15,10"
                       Background="#F44336"
                       Foreground="White"
                       Visibility="Collapsed"/>
            </StackPanel>
        </Border>
        
        <ListView x:Name="SuppliersListView" 
                 Grid.Row="1"
                 SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding SupplierID}"/>
                    <GridViewColumn Header="Название" Width="200" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Контактная информация" Width="250" DisplayMemberBinding="{Binding ContactInfo}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
