<Page x:Class="Practice2025One.Pages.UsersManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Управление пользователями"
      Loaded="Page_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Панель управления -->
        <Border Grid.Row="0" Background="White" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="Добавить пользователя" 
                       Click="AddUserButton_Click"
                       Padding="15,10" Margin="0,0,10,0"
                       Background="#4CAF50"
                       Foreground="White"/>
                <Button Content="Редактировать" 
                       Click="EditUserButton_Click"
                       Padding="15,10" Margin="0,0,10,0"
                       Background="#2196F3"
                       Foreground="White"/>
                <Button Content="Изменить роль" 
                       Click="ChangeRoleButton_Click"
                       Padding="15,10" Margin="0,0,10,0"
                       Background="#FF9800"
                       Foreground="White"/>
                <Button Content="Удалить" 
                       Click="DeleteUserButton_Click"
                       Padding="15,10"
                       Background="#F44336"
                       Foreground="White"/>
            </StackPanel>
        </Border>
        
        <ListView x:Name="UsersListView" 
                 Grid.Row="1"
                 SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding UserID}"/>
                    <GridViewColumn Header="ФИО" Width="220" DisplayMemberBinding="{Binding FullName}"/>
                    <GridViewColumn Header="Email" Width="220" DisplayMemberBinding="{Binding Email}"/>
                    <GridViewColumn Header="Телефон" Width="150" DisplayMemberBinding="{Binding Phone}"/>
                    <GridViewColumn Header="Роль" Width="150" DisplayMemberBinding="{Binding RoleName}"/>
                    <GridViewColumn Header="Активен" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive}" IsEnabled="False"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Дата регистрации" Width="150" DisplayMemberBinding="{Binding CreatedAt}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
