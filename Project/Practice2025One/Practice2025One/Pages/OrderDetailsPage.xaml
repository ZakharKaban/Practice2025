<Page x:Class="Practice2025One.Pages.OrderDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Детали заказа"
      Loaded="Page_Loaded">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="Информация о заказе" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Номер заказа:" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="OrderNumberTextBlock" Margin="0,0,0,10"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Дата заказа:" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="OrderDateTextBlock" Margin="0,0,0,10"/>
                    
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Адрес доставки:" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="DeliveryAddressTextBlock" TextWrapping="Wrap" Margin="0,0,0,10"/>
                    
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Статус:" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="StatusTextBlock" Margin="0,0,0,10"/>
                    
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Общая стоимость:" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" x:Name="TotalPriceTextBlock" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                </Grid>
                
                <TextBlock Text="Товары в заказе:" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                
                <ListView x:Name="OrderItemsListView">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Название" Width="{Binding ActualWidth, 
    RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListView}}}" DisplayMemberBinding="{Binding ProductName}"/>
                            <GridViewColumn Header="Количество" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Quantity}" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Цена за единицу" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding PriceAtMoment}" HorizontalAlignment="Right"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Общая стоимость" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding TotalPrice}" HorizontalAlignment="Right" FontWeight="Bold"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
                
                <Button Content="Назад" 
                       Click="BackButton_Click"
                       HorizontalAlignment="Left"
                       Margin="0,20,0,0"
                       Padding="15,10"
                       Width="120"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
