<Page x:Class="DemoEx.Pages.PageCatalog"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DemoEx.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageCatalog">

    <Grid>
        <ListView x:Name="listViewMain" HorizontalAlignment="Left" Height="351" Margin="92,0,0,0" VerticalAlignment="Center" Width="669" d:ItemsSource="{d:SampleData ItemCount=5}" SelectionMode="Single" SelectionChanged="listViewMain_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0" Source="{Binding ImagePath}" Width="100" Height="100" />

                        <StackPanel Grid.Column="1">
                            <TextBlock>
                      
                        <Run Text="{Binding Suppliers.Name}"/>
                        
                            </TextBlock>
                        </StackPanel>

                        <Button Grid.Column="2" x:Name="buttonDelete" Width="50" Height="20" Content="Удалить" Click="buttonDelete_Click"></Button>
                    </Grid>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


    </Grid>
</Page>
